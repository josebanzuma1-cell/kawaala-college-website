# Operating system clutter
Thumbs.db
desktop.ini
.DS_Store

# Editor files
*.swp
*~

# Original, full-size photography. Kept on the office computer, not in the
# repository: it is around 27 MB of source material that the website itself
# never loads, and GitHub Pages would publish every byte of it.
# Keep your own backup of this folder somewhere safe.
assets/source/

# Build scratch
manifest.json
